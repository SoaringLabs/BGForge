# BGForge

BGForge 是由 **Soaring Labs** 开发、基于 **BGLite 2.4.0** 的《魔兽世界》团本拍卖增强插件。

项目当前处于早期验证阶段，目标是在保持安全、稳定和基础拍卖能力的前提下，逐步补充更完整的团本拍卖与记账体验。

游戏内插件名称：`<BGForge> 团本事务台`

当前版本：`1.0.1`

版本变更参见 [CHANGELOG.md](CHANGELOG.md)。

## 当前状态

v0.3.0 扩展了全角色总览：

- 新增团本周常、祖格周常及珠宝、烹饪、钓鱼专业日常完成状态；
- 周常与专业日常使用二级表头，并可按组控制显示；
- 任务完成状态仅保存在本机角色总览记录中，不影响拍卖功能。

当前面向 Titan Reforged Classic 的精简装备过滤支持：

- 按当前职业手动选择职业或专精方案，不自动读取当前天赋；
- 不适合方案的装备会在当前表格、个人心愿单、自动拍卖记录和正在拍卖窗口中置灰；
- 当前表格与个人心愿单共用同一组装备过滤选择，任一页面切换后会同步刷新；
- 可在自动拍卖设置中启用“被过滤的装备自动折叠”，对任一过滤方案生效；
- 战网账号绑定装备始终保持高亮；
- 只保存按职业选择的方案，不保存角色名、服务器、GUID、天赋或物品记录；
- 不影响拾取通知或装备库。

Titan 本地历史拍卖表格当前已软下线：游戏内不提供保存、浏览或管理入口，
新 CD 自动清空表格时也不会生成历史记录。实现源码与旧存档结构暂时保留，
便于后续根据官方规则调整。

当前面向 Titan Reforged Classic 的个人心愿单支持：

- 按副本和 BOSS 浏览实际掉落并加入或移除心愿；散件直接显示，套装等兑换装备只显示 Boss 掉落的兑换物；
- 职业套装兑换物会跨 BOSS 去重并按可兑换职业分组，当前职业组默认展开，其他职业组可按需展开；
- BOSS 掉落使用独立卡片展示，并以实际物品品质颜色区分橙装、紫装、蓝装等；
- 心愿掉落时提供独立文字和语音提醒，不受自动记账开关影响；
- 心愿装备在拍卖中保持展开并显示“心愿”标记；
- 仅在交易确认或额外奖励确认获得时自动移除，普通掉落不会自动删除；
- 数据仅保存在本机当前角色下，不收集团队成员资料，不提供同步、广播、导入或导出。

![BGForge Titan 装备过滤](docs/images/v0.2.0-spec-gear-filter.png)

此前已经完成 BGForge 的独立识别和基础品牌调整：

- 独立插件目录与入口文件 `BGForge/BGForge.toc`
- 插件列表、主界面、设置页及小地图入口统一使用 BGForge 名称
- 简体中文、繁体中文和英文基础说明同步更新
- 资源路径由 `BGLite` 调整为 `BGForge`
- 保留 BGLite 2.4.0 通信版本，便于后续验证兼容性
- 暂时保留原有 `BG` 全局对象和 `BiaoGe` 存档结构，降低早期改造风险

这一阶段以验证独立运行路线为主，尚未对核心拍卖流程进行大规模重构。

## 使用命令

在游戏聊天框中输入：

```text
/bgf
```

即可打开或关闭主界面。

目前还保留以下兼容命令：

```text
/bglite
/gbg
/biaoge
```

## 安装

1. 从 GitHub 下载或克隆本仓库。
2. 确保最终插件文件夹名称为 `BGForge`。通过 GitHub 下载 ZIP 时，可能需要把解压后的 `BGForge-main` 重命名为 `BGForge`。
3. 将整个 `BGForge` 文件夹放入对应魔兽客户端的插件目录：

   ```text
   World of Warcraft/<客户端目录>/Interface/AddOns/BGForge
   ```

4. 完全退出并重新启动游戏。
5. 在角色选择界面的插件列表中启用 `<BGForge> 团本事务台`。
6. 进入游戏后输入 `/bgf` 打开插件。

安装后的目录结构应类似：

```text
Interface/
└── AddOns/
    └── BGForge/
        ├── BGForge.toc
        ├── Core/
        ├── Libs/
        ├── Locales/
        └── Media/
```

## 与 BGLite、BiaoGe 的关系

BGForge 当前是基于 BGLite 源码发展的独立增强版本，不是需要同时加载 BGLite 的附加模块。

测试和使用 BGForge 时，请禁用 `BGLite` 与 `BiaoGe`。当前版本仍与它们共用部分全局对象、窗口名称、命令入口及存档结构，同时启用可能导致：

- 插件初始化顺序相互覆盖
- 主界面或按钮失效
- 拍卖消息被重复处理
- 设置和账本数据发生混乱

不需要删除原插件，在角色选择界面的插件列表中取消勾选即可。

## 开发方式

仓库根目录本身就是可安装的插件目录。修改代码后，将本目录同步到游戏的 `Interface/AddOns/BGForge`，然后在游戏内输入：

```text
/reload
```

即可加载大多数 Lua、XML 和本地化文本修改。首次安装、修改目录名称或 `.toc` 元数据后，完全重启客户端更稳妥。

## 后续方向

- 验证现有拍卖、记账和交易功能的独立运行稳定性
- 优化界面文案与基础交互体验
- 逐步增加实用的团本拍卖增强功能
- 梳理与上游版本的兼容边界
- 在功能稳定后再评估全局变量和存档结构迁移

## 问题反馈

如遇到 Lua 报错、界面异常或功能问题，请在 [GitHub Issues](https://github.com/SoaringLabs/BGForge/issues) 中提交，并尽量附上：

- 游戏客户端版本
- BGForge 版本
- 问题复现步骤
- BugSack、BugGrabber 或游戏错误窗口中的完整报错信息

## 致谢与来源

BGForge 基于 BGLite 2.4.0 继续开发。BGLite 基础版本由《魔兽世界》国服运营团队进行安全清理与精简。

BGForge 是由 Soaring Labs 维护的独立第三方项目，不代表《魔兽世界》官方立场。
